import React, { FC } from "react";
import { Box } from "@chakra-ui/react";
import { RollingCookie } from "@animations";

export const BiscuitMachine: FC = () => {

  return (
    <Box>
      <RollingCookie />
    </Box>
  );
};
